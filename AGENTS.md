# VLA Wrench Control ROS 2

Allows controlling a UR5e in end effector wrench space using ROS 2 cartesian controllers and optinally mujoco for simulation.

IMPORTANT: Always use `pixi run` to run commands inside the ROS 2 conntext.

Build: `pixi run build`

Run: `pixi run demo`
